server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/meta-tsq/microservice-config.git
          default-label: main
          skip-ssl-validation: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka

